<link href="../css/select2.css" rel="stylesheet"/>
<script src="../js/select2.js"></script>
<div class="page-header">
    <h1>Create Discount</h1>
</div>
<form action="<?php echo HOST ?>product/discount-save" class="form-horizontal">
    <div class="control-group good_has_bonus" >
        <label class="control-label" for="good_id">Product</label>
        <div class="controls">
            <select name="good_id" id="good_id" class="span3">
                <option value>---</option>
                <?php if (isset($this->goods) && $this->goods): ?>
                    <?php foreach ($this->goods as $key => $value): ?>
                        <option value="<?php echo $key ?>"
                            <?php if ( isset($this->bundle_gift['good_id']) && $this->bundle_gift['good_id'] == $key ) echo 'selected' ?>><?php echo htmlentities(htmlspecialchars($value), ENT_QUOTES) ?></option>
                        <?php endforeach ?>
                    <?php endif ?>
                </select>
            </div>
        </div>
        <div class="control-group bonus_good">
            <label class="control-label">Discount For :</label>
            <div class="controls">
              <select name="name" class="name span3">
                <option value="<?php echo $this->bundle_gift['name'] ?>"><?php echo $this->bundle_gift['name'] ?></option>
                <option value="DEMO">DEMO</option>
                <option value="Staff">Staff</option>
                <option value="APK For LTH">APK For LTH</option>
                <option value="Free Stuff">Free Stuff</option>
            </select>
        </div>
    </div>

    <div class="control-group">
        <label class="control-label">Discount ( % ສ່ວນຫຼຸດ )</label> 
        <div class="controls">
            <input type="text" name="discount" class="discount span3" value="<?php echo $this->bundle_gift['discount'] ?>" placeholder="Plase Input !!!!" required />
        </div>
    </div>


    <div class="form-actions">
        <input type="hidden" name="id" id="id" value="<?php if(isset($this->id) && $this->id) echo $this->id ?>" />
        <button type="submit" class="btn btn-primary">Save</button>
        <button type="button" class="btn btn-danger" onclick="window.location='<?php echo HOST ?>product/discount'">Go Back</button>
    </div>
</form>

<script>

    $(document).ready(function() {
        $("#good_id").select2();
        $('#good_id').change(good_change);
        $('.btn_add').click(init_add);
        $('.btn_remove').click(remove_bonus);
    });

</script>